# Specification

## Summary
**Goal:** Publish (run) Draft Version 26 so it becomes the active preview build for testing via the standard preview link.

**Planned changes:**
- Publish Draft Version 26 as the currently running preview build.

**User-visible outcome:** Opening the standard preview URL loads the app running Draft Version 26.
